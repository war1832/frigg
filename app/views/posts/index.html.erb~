<h1>Listing Posts</h1>
<% @posts.each do |post| %>
  <h2><%= post.title %></h2>
  <%= truncate(post.body, :length => 100, :escape => false) %>
  <%= show_link post %>
<hr>
<% end %>
<%= will_paginate @posts, renderer: BootstrapPagination::Rails %>
